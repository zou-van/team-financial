# Project State

Last updated: 2026-05-01

## What's Built

Team financial data dashboard — static SPA with Vite + React. Parses monthly Excel files into Markdown, bundles at build time via `import.meta.glob`.

### Two views
- **月度详情** — single big table, metrics as rows, teams as columns, 3-level headers (Leader → Sub Team → Individual Team)
- **趋势图** — line chart (Recharts) with curated metrics, dimension picker (大团队/子团队/小团队)

### Features implemented
- Excel → Markdown parsing (`scripts/parse.py`)
- Monthly table with grouped headers (`src/components/MonthlyTable.jsx`)
- Trend chart with metric dropdown + dimension picker (`src/components/TrendChart.jsx`)
- Per-metric-row collapse/expand toggle (click "−"/"+" button)
- Aggregate columns (subteam/boss) styled bold with blue background
- Negative numbers displayed in red
- Name deduplication for duplicate Excel headers (in both parse.py and loadData.js)
- Trend chart: curated metrics via `trend_metrics` in team-mapping.yaml
- Trend chart: pre-aggregated data in `data/trend.json` (generated by parse.py)
- Trend chart: dimension picker — 大团队 (1 line), 子团队 (small teams solid + aggregate dashed), 小团队 (1 line)
- Trend chart: numbers formatted with thousand separators, rounded to integers, unit "千元"
- Trend chart: name collision handling when sub-team shares name with small team (e.g. 前端创新)

## Uncommitted Changes

None — all changes committed.

## Data Pipeline

```
excel/*.xlsx → scripts/parse.py → data/YYYY-MM.md + data/index.json + data/trend.json
                                         ↓
                               Vite import.meta.glob (eager, ?raw)
                                         ↓
                           src/lib/loadData.js → parseMarkdown.js
                                         ↓
                                 React components
```

## Key Files

| File | Role |
|------|------|
| `scripts/parse.py` | Excel → Markdown + index.json + trend.json |
| `team-mapping.yaml` | Org hierarchy + trend_metrics whitelist |
| `src/lib/loadData.js` | Build-time data loading, column grouping |
| `src/lib/parseMarkdown.js` | Markdown → JS objects |
| `src/App.jsx` | Root component, month/view state |
| `src/components/MonthlyTable.jsx` | Monthly detail table |
| `src/components/TrendChart.jsx` | Trend line chart with dimension picker |
| `src/App.css` | All styles |

## Commands

| Command | Purpose |
|---------|---------|
| `npm run dev` | Dev server on port 5173 |
| `npm run build` | Production build to `dist/` |
| `python scripts/parse.py` | Parse Excel files → data/ |

## Known Constraints

- Target Excel sheet hardcoded as `差距分析(团队)`
- Name dedup logic exists in TWO places (`parse.py` + `loadData.js`), must stay in sync
- `assetsInclude: ['**/*.md']` in `vite.config.js` is required for .md imports
- Two months of data (2026-02, 2026-03) available; trend chart works but limited to one month of delta
- Trend chart: when sub-team name collides with small team name, aggregate line uses deduped key (e.g. `前端创新(2)`) and displays as `前端创新(子团队)`
